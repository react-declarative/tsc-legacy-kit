namespace app {

    const {
        Typography,
    } = sdk.mui.material;

    export namespace components {
        export const App = () => (
            <Typography>
                Hello world!
            </Typography>
        )
    }
}